# Core Output Rules

1. Every phase writes its result under `output/docs/`.
2. Every phase output must include: Objective, Inputs Used, Key Decisions, Risks and Assumptions, Open Questions, and Handoff.
3. Do not hide uncertainty. Record it in `Open Questions`.
4. Keep outputs implementation-oriented and traceable to earlier artifacts.
